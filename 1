void reverse(int arr[],int l,int r ){
    while(l<=r){
    swap(arr[l],arr[r]);
        l++;
        r--;
    }
   
}
int main(){
    int arr[]={1,2,3,4};
    int n=sizeof(arr)/sizeof(arr[0]);
    reverse(arr,0,n-1);
    for(int i=0;i<n;i++){
        cout<<arr[i]<<endl;
    }
    
}
