void reverse(int arr[],int i,int n){
    while(i<n/2){
    swap(arr[i],arr[n-i-1]);
        i++;
    }
   
}
int main(){
    int arr[]={1,2,3,4};
    int n=sizeof(arr)/sizeof(arr[0]);
    reverse(arr,0,n);
    for(int i=0;i<n;i++){
        cout<<arr[i]<<endl;
    }
    
}
